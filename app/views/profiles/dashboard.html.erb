<% content_for :title, "Edit profile" %>
<div class="row-fluid">
	<h4>Hello <%= @user.name.presence || @user.email %></h4>

	<%= link_to "Edit your profile", edit_user_registration_path(current_user), :class => 'btn btn-warning'%>
</div>